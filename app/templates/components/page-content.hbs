{{#if page.bodyHTML}}
  <div class="content-box">
  {{#if page.isPage}}<h1 class="page-title">{{page.title}}</h1>{{/if}}
  {{{page.bodyHTML}}}
  </div>
{{else}}
  {{#if page.isPage}}
    <div class="content-box">
      <h1 class="page-title">{{page.title}}</h1>
    </div>
  {{/if}}
{{/if}}

{{yield}}
